apiVersion: v1
kind: Service
metadata:
  name: qemu-ios-emulator-service
spec:
  type: ClusterIP
  selector:
    app: qemu-ios-emulator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
