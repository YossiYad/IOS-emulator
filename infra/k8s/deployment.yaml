apiVersion: apps/v1
kind: Deployment
metadata:
  name: silicon-emulator-manager
  labels:
    app: silicon-emulator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: silicon-emulator
  template:
    metadata:
      labels:
        app: silicon-emulator
    spec:
      containers:
        - name: manager
          image: silicon-emulator/manager:latest
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: images
              mountPath: /workspaces/images
            - name: logs
              mountPath: /var/log/silicon-emulator
      volumes:
        - name: images
          emptyDir: {}
        - name: logs
          emptyDir: {}
